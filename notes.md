#These are some notes
that i will make.
Generally it is super important to always pull from git hub first whenever
I start working, otherwise stuff will get out of sync.
That also means I always should push when finishing work.

##Markdown Notes:
-there are three headings, marked by "#" "##" "###" respectively
-bold text is marked by _TEXT_
-italic text is marked by **TEXT**
-a block quote is created by >TEXT
-code is inserted by ` <p>text</p>`
